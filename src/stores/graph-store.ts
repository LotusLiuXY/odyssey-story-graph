import type { NodeKind } from "@/lib/graph/data";

// Placeholder module retained for import stability; view state lives locally
// in the graph screen. Not used at runtime.
export type UnusedKind = NodeKind;
